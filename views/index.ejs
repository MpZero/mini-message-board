<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= title%></title>
  </head>
  <body>
    <%- include("./navbar") %>
    <h1>Homepage</h1>
    <ul>
      <%messages.map((message, index) =>{ %>
      <li>
        <p><b>User:</b> <%= message.username %></p>
        <p><b>Message:</b> <%= message.message %></p>
        <p><b>Added on:</b> <%= message.date.toDateString() %></p>
        <a href="/new/<%= index +1 %>"> Show details</a>
      </li>
      <%})%>
    </ul>
  </body>
</html>
